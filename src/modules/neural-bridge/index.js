/**
 * Neural Bridge
 * Spawns python tensorflow scripts and uses websockets to communicate with them.
 */

module.exports = {
    spawn: require('./spawn'),
    // websocket: require('./websocket'),
};
